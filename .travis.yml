language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t mingyen066/poc-docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mingyen066/poc-docker-react npm run test